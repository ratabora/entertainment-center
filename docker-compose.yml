version: '3.3'
services:
  plexpy:
    image: "linuxserver/plexpy:latest"
    volumes:
      - /tmp/plex/config/Library/Application\ Support/Plex\ Media\ Server/Logs:/logs
      - /tmp/plex/config:/config
    ports:
      - "10002:8181"
  plex:
    image: "linuxserver/plex:latest"
    env_file:
      - plex.env
    volumes:
      - /tmp/plex/config:/config
      - /tmp/plex/transcode:/transcode
      - /tmp/media:/data
    ports:
      - "10005:32400"
    expose:
      - "32400"
